# Hervanta Housing Manager Map

Map of selected Hervanta housing-company managers.

The publishable GitHub Pages files are in `docs/`.

- `docs/index.html` is the dynamic zoomable OpenStreetMap version.
- `docs/offline.html` is the offline raster snapshot using locally saved tiles.
